# 🚀 部署指南

这个工具是一个 Python Flask 应用，可以通过多种方式部署。

---

## 📝 方案一：本地运行（最简单）

这是最推荐的方式，因为音频处理比较消耗资源，本地运行最快最稳。

### 操作步骤：

1. **安装 Python**
   - 下载：https://www.python.org/downloads/
   - 安装时勾选 "Add Python to PATH"

2. **下载项目**
   - 打开：https://github.com/zwmopen/subliminal-audio-synthesizer
   - 点击绿色 "Code" 按钮
   - 选择 "Download ZIP"
   - 解压到你想放的文件夹

3. **安装依赖**
   ```bash
   cd 解压后的文件夹
   pip install -r requirements.txt
   ```

4. **启动应用**
   ```bash
   python subliminal_master.py
   ```
   或者直接双击 `启动Subliminal_Master.bat`

5. **使用**
   - 浏览器打开：http://localhost:5000
   - 就可以开始用了！

---

## 🌐 方案二：部署到免费云服务

### 选项 A：Railway（最适合 Flask）

Railway 对 Python 支持很好，有免费额度。

1. 注册账号：https://railway.app
2. 点击 "New Project" → "Deploy from repo"
3. 选择你的 GitHub 仓库
4. 点击 "Deploy"
5. 等待部署完成，会给你一个网址

### 选项 B：Fly.io

Fly.io 也有免费额度，适合长期运行。

1. 注册账号：https://fly.io
2. 安装 flyctl：https://fly.io/docs/getting-started/installing-flyctl/
3. 在项目目录运行：
   ```bash
   fly launch
   fly deploy
   ```

### 选项 C：Render

Render 简单易用，免费额度够小项目用。

1. 注册账号：https://render.com
2. 点击 "New +" → "Web Service"
3. 连接你的 GitHub 仓库
4. 配置：
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python subliminal_master.py`
5. 点击 "Create Web Service"

---

## 🖥️ 方案三：自己的服务器（Nginx + Gunicorn）

如果你有自己的 VPS，这是最稳定的方案。

### 步骤：

1. **安装依赖**
   ```bash
   sudo apt update
   sudo apt install python3-pip nginx ffmpeg
   pip3 install gunicorn
   ```

2. **上传代码到服务器**
   ```bash
   # 用 scp 或 git clone
   git clone https://github.com/zwmopen/subliminal-audio-synthesizer.git
   cd subliminal-audio-synthesizer
   pip3 install -r requirements.txt
   ```

3. **用 Gunicorn 启动**
   ```bash
   gunicorn -w 4 -b 127.0.0.1:5000 subliminal_master:app
   ```

4. **配置 Nginx**
   ```nginx
   server {
       listen 80;
       server_name 你的域名;
       
       location / {
           proxy_pass http://127.0.0.1:5000;
           proxy_set_header Host $host;
           proxy_set_header X-Real-IP $remote_addr;
       }
       
       client_max_body_size 200M;
   }
   ```

5. **用 systemd 守护进程**
   ```service
   [Unit]
   Description=Subliminal Audio Synthesizer
   After=network.target
   
   [Service]
   User=www-data
   WorkingDirectory=/path/to/project
   ExecStart=/usr/local/bin/gunicorn -w 4 -b 127.0.0.1:5000 subliminal_master:app
   Restart=always
   
   [Install]
   WantedBy=multi-user.target
   ```

---

## 📌 重要提示

### 免费云服务的限制

| 服务 | 免费额度 | 注意 |
|-----|---------|------|
| Railway | $5/月 | 音频处理可能超额度 |
| Fly.io | 免费机 | 音频处理慢，可能超时 |
| Render | 750小时/月 | 适合小流量使用 |

### 为什么推荐本地运行？

1. **速度快**：音频处理消耗 CPU，本地机器比免费云快得多
2. **不花钱**：不需要给云服务付费
3. **隐私好**：你的音频文件只在你电脑上
4. **稳定**：不会因为超额度被关停

---

## 💡 最佳实践

**如果你想分享给朋友用：**
- 自己本地运行
- 用内网穿透工具（如 ngrok）分享临时链接

**如果你想做一个公开网站：**
- 买个便宜的 VPS（阿里云/腾讯云/华为云，几十块钱一个月）
- 部署到自己的服务器

---

有问题？看 `快速上手指南.md`！
